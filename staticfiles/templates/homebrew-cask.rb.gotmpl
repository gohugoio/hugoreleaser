cask "{{ .Token }}" do
  version "{{ .Version }}"
  sha256 "{{ .SHA256 }}"

  url "{{ .URL }}"
  name "{{ .Name }}"
  desc "{{ .Desc }}"
  homepage "{{ .Homepage }}"

  pkg "{{ .Pkg }}"
{{- if .Binary }}

  binary "{{ .Binary }}"
{{- end }}

  uninstall pkgutil: "{{ .BundleIdentifier }}"
end
